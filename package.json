{
  "name": "agi",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -k -n be,fe -c blue,magenta \"npm --prefix backend run dev\" \"wait-on tcp:3000 && npm --prefix frontend run dev\"",
    "start": "concurrently -k -n be,fe -c blue,magenta \"npm --prefix backend run dev\" \"wait-on tcp:3000 && npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "wait-on": "^8.0.0"
  }
}
